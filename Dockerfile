# Выбор языка программирования и версия latest
FROM node:20

# Определения рабочей папки внутри Docker
WORKDIR /social49

# Копируем все папки+файлы с локального на комп Docker
COPY . /social49

# Устанавливаем наши зависимости
RUN npm install

# Выбираем порт для запуска внутри Docker
EXPOSE 3000

# Итоговая команда для запуска проекта
CMD ["npm", "run", "dev"]

